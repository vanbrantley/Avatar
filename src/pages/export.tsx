import AvatarExport from "@/components/AvatarExport";

const Export = () => {

    return (
        <div className="flex items-center justify-center">
            <AvatarExport mini={false} />
        </div>
    );

};

export default Export;